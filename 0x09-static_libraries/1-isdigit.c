#!/bin/bash
#include "main.h"

/**
 * _isdigit - checks if a value is a digit
 * @c: is an integer param
 *
 * Description: This function checks if it is a digit
 * Return: Always 0.
 */


int _isdigit(int c)
{

	return (c >= 48 && c <= 57);
}
